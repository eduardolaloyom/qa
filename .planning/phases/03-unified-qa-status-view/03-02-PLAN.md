---
phase: 03-unified-qa-status-view
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - public/index.html
autonomous: true
requirements:
  - DASH-03
  - DASH-04
user_setup: []

must_haves:
  truths:
    - "Usuario ve una fila por cliente en la sección 'Estado QA por Cliente' del tab B2B, con 3 badges por fila (Playwright, Cowork, Maestro)"
    - "Playwright badge muestra `{pct}%` con clase pw-ok (100%), pw-warn (70-99%), pw-fail (<70%), o u-na (tests=0)"
    - "Cowork badge muestra LISTO / CON CONDICIONES / BLOQUEADO / 'Sin Cowork' según manifest.json (platform != 'app', match por client_slug lowercase)"
    - "Maestro badge muestra `{health}/100` con clase mt-* solo si slug in appClients=['prinorte','surtiventas','coexito'], else u-na"
    - "Cuando `daysDiff(run.date, sourceDate) > 2`, el badge agrega el sufijo `<span class=\"u-badge-stale-suffix\">Hace N días</span>` inline"
    - "Filtros pills funcionan: 'Todos' muestra todas las filas; 'Con problemas' oculta filas sin ningún pw-fail/cw-bloqueado/mt-fail; 'Stale' oculta filas sin ningún sufijo stale"
    - "Cambiar `#runSelector` (Phase 2) re-renderiza la tabla con el nuevo run Y resetea el filter pill a 'Todos' (D-12)"
    - "Filas ordenadas alfabéticamente por `c.name` ascending (D-13)"
    - "Al no haber datos en selectedRun.clients, la tabla muestra 'Sin datos en este run. Ejecuta /run-playwright para poblar la tabla.'"
  artifacts:
    - path: "public/index.html"
      provides: "updateUnifiedQaTable orchestrator + 3 badge renderers + daysDiff/staleSuffixHtml helpers + setupUnifiedFilterPills + resetUnifiedFilterPills + loadManifestCached + appClients constant + integration in initDashboard + second #runSelector listener"
      contains_js: ["function updateUnifiedQaTable(run = latestRun)", "function renderPlaywrightBadge(", "function renderCoworkBadge(", "function renderMaestroBadge(", "function daysDiff(", "function staleSuffixHtml(", "function setupUnifiedFilterPills(", "function resetUnifiedFilterPills(", "function loadManifestCached(", "const appClients", "let cachedManifest"]
  key_links:
    - from: "initDashboard()"
      to: "updateUnifiedQaTable(latestRun) + setupUnifiedFilterPills()"
      via: "Direct calls después de updateClients() y populateRunSelector()"
      pattern: "updateUnifiedQaTable\\(|setupUnifiedFilterPills\\("
    - from: "#runSelector change event (listener adicional, D-01)"
      to: "loadRunDetails(date) -> updateUnifiedQaTable(run) + resetUnifiedFilterPills()"
      via: "addEventListener('change', ...) separado del listener de Phase 2"
      pattern: "updateUnifiedQaTable\\(run\\).*resetUnifiedFilterPills"
    - from: "updateUnifiedQaTable()"
      to: "loadManifestCached() -> render3badges -> tbody.innerHTML"
      via: "await loadManifestCached() + Object.entries(run.clients).sort().map(...)"
      pattern: "loadManifestCached\\(\\)"
    - from: "renderPlaywrightBadge / renderCoworkBadge / renderMaestroBadge"
      to: "staleSuffixHtml(run.date, sourceDate) via daysDiff"
      via: "Helper compartido"
      pattern: "staleSuffixHtml\\("
    - from: "setupUnifiedFilterPills event delegation"
      to: "tbody.classList.add('filter-problemas'|'filter-stale')"
      via: "Click handler sobre #unifiedFilterPills container"
      pattern: "classList\\.add\\(`filter-\\$\\{mode\\}`\\)|classList.add\\('filter-(problemas|stale)'\\)"
---

<objective>
Cablear la tabla unificada (DASH-03) con data real de `selectedRun.clients` + `manifest.json`, incluir el sufijo stale por badge (DASH-04), y wire los 3 filter pills (D-08 a D-12). Integrar con el `#runSelector` existente de Phase 2 agregando un LISTENER ADICIONAL (D-01 — no modifica el listener de Phase 2). Llamar `updateUnifiedQaTable(latestRun)` y `setupUnifiedFilterPills()` desde `initDashboard()`.

Purpose: Convierte el skeleton CSS+HTML de Plan 01 en una tabla viva que muestra "¿está este cliente QA-listo?" combinando Playwright + Cowork + Maestro. Es la entrega principal de Phase 3 — las 3 pipelines unificadas en una sola vista. Cumple DASH-03 (la sección con 3 badges por cliente) y DASH-04 (indicador stale por badge).

Output: ~10 nuevas funciones JS en el bloque `<script>` de `public/index.html`, 1 constante (`appClients`), 1 variable de cache (`cachedManifest`), 3 invocaciones en `initDashboard`, 1 listener adicional sobre `#runSelector`. Total ~130 líneas agregadas de JS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-unified-qa-status-view/03-CONTEXT.md
@.planning/phases/03-unified-qa-status-view/03-RESEARCH.md
@.planning/phases/03-unified-qa-status-view/03-UI-SPEC.md
@.planning/phases/02-data-freshness-signals/02-02-SUMMARY.md
@.planning/phases/03-unified-qa-status-view/03-01-PLAN.md
@public/index.html
@public/manifest.json
@public/history/2026-04-17.json

<interfaces>
<!-- APIs y data contracts que el executor necesita — extraídos directamente del código existente -->

Existing globals in <script> (verified in public/index.html):
```javascript
// Line 1412-1413:
let latestRun = null;
let allRuns = [];

// Line 1475 (async):
async function loadRunDetails(date) { /* fetch(`history/${date}.json`) */ }

// Line 1471:
// loadHistoryIndex() returns allRuns[]

// Line 1481-1503:
async function initDashboard() {
    // ... sequence ...
    updateClients();             // Phase 2 (line 1495)
    populateRunSelector();       // Phase 2 (line 1496)
    updateTrendChart();          // line 1497
    // Phase 3 MUST insert: setupUnifiedFilterPills(), updateUnifiedQaTable(latestRun),
    //                      and the additional #runSelector listener AFTER populateRunSelector returns
}

// Line 1863:
function updateClients(run = latestRun) { /* Phase 2 */ }

// Line 1945-1967:
function populateRunSelector() {
    const sel = document.getElementById('runSelector');
    // ...
    sel.addEventListener('change', (e) => {
        loadRunDetails(e.target.value).then(run => {
            if (run) updateClients(run);       // Phase 2 listener — Phase 3 ADDS a separate one
        });
    });
}

// Line 2040:
function escapeHtml(str) { /* existing helper — USE this for all user-derived strings */ }
```

Data contracts (VERIFIED by inspection):

```typescript
// public/history/{YYYY-MM-DD}.json
{
    date: "2026-04-17",            // string YYYY-MM-DD — NO hora
    total: number, passed: number, failed: number,
    clients: {
        [slug: string]: {
            name: string,           // ej "Codelpa", "Sonrie (staging)"
            tests: number,
            passed: number,
            failed: number,
            last_tested: "2026-04-17" | null  // YYYY-MM-DD or null
        }
    }
}

// public/manifest.json
{
    reports: [
        {
            client_slug: string,    // ej "codelpa", "prinorte" — case puede variar
            client: string,
            date: "2026-04-17",     // YYYY-MM-DD
            platform: "b2b" | "app",
            verdict?: string,       // "LISTO" | "CON_CONDICIONES" | "CON CONDICIONES" | "BLOQUEADO" | "NO_APTO" (Cowork only)
            health?: number,        // Maestro post-PIPE-01
            score?: number,         // Maestro legacy — fallback: rep.health ?? rep.score
            // ... other fields irrelevant to Phase 3
        }
    ]
}
```

Skeleton from Plan 01 (must exist before this plan runs):
- `<tbody id="unifiedQaBody">` with a loading row
- `<div id="unifiedFilterPills">` with 3 buttons having `data-filter="all|problemas|stale"`
- All CSS classes (.u-badge.*, .unified-filter-pill*, filter-problemas/filter-stale rules)

Verified patterns from Phase 2 (SUMMARY):
- `Math.round((new Date(referenceDate) - new Date(c.last_tested)) / 86400000)` for days diff
- Default parameter `run = latestRun` for backward compat
- No mutation of `latestRun` in listener body
- Template literal HTML with `escapeHtml(...)` on user-derived strings
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: JS helpers + 3 badge renderers + updateUnifiedQaTable orchestrator + manifest cache</name>
  <files>public/index.html</files>
  <read_first>
    - public/index.html (lectura completa; especialmente líneas 1410-1500 initDashboard, 1863-1945 updateClients, 2040-2045 escapeHtml, 2354-2450 loadCoworkReports/loadAppReports — patrón de match por client_slug)
    - public/manifest.json (data real para validar el match case-insensitive y el formato de verdict/health)
    - public/history/2026-04-17.json (clients[slug] shape real)
    - .planning/phases/03-unified-qa-status-view/03-UI-SPEC.md (secciones Component Inventory 2a/2b/2c + Matriz de Condiciones)
    - .planning/phases/03-unified-qa-status-view/03-RESEARCH.md (Code Examples: renderPlaywrightBadge/Cowork/Maestro, Pitfalls 3/4/5)
    - .planning/phases/03-unified-qa-status-view/03-CONTEXT.md (D-03, D-13, D-15)
    - .planning/phases/03-unified-qa-status-view/03-01-PLAN.md (clases CSS y IDs DOM disponibles)
  </read_first>
  <action>
  **Ubicación de inserción:** Agregar todo el JS nuevo en el bloque `<script>` de `public/index.html`. Las piezas van en el siguiente orden, ubicadas DESPUÉS de la declaración `let allRuns = [];` (línea 1413 original) y ANTES de `async function loadHistoryIndex()`.

  **Paso 1 — Agregar constantes globales justo después de `let allRuns = [];`:**

  ```javascript
  // ── Phase 3: Unified QA Status View ──────────────────────────────────────
  const appClients = ['prinorte', 'surtiventas', 'coexito'];  // MEMORY.md project_app_clients
  let cachedManifest = null;

  async function loadManifestCached() {
      if (cachedManifest) return cachedManifest;
      try {
          const r = await fetch('manifest.json?v=' + Date.now());
          if (!r.ok) throw new Error('no manifest');
          cachedManifest = await r.json();
          return cachedManifest;
      } catch {
          return { reports: [] };
      }
  }

  function daysDiff(fromDateISO, toDateISO) {
      if (!fromDateISO || !toDateISO) return null;
      return Math.round((new Date(fromDateISO) - new Date(toDateISO)) / 86400000);
  }

  function staleSuffixHtml(runDate, sourceDate) {
      const d = daysDiff(runDate, sourceDate);
      if (d === null || d <= 2) return '';
      const label = d === 1 ? 'Hace 1 día' : `Hace ${d} días`;
      return ` <span class="u-badge-stale-suffix">${label}</span>`;
  }

  function renderPlaywrightBadge(c, runDate) {
      if (!c || !c.tests) {
          return '<span class="u-badge u-na">N/A</span>';
      }
      const pct = Math.round((c.passed / c.tests) * 100);
      const cls = pct === 100 ? 'pw-ok' : pct >= 70 ? 'pw-warn' : 'pw-fail';
      const stale = staleSuffixHtml(runDate, c.last_tested);
      return `<span class="u-badge ${cls}">${pct}%${stale}</span>`;
  }

  function renderCoworkBadge(slug, runDate, manifest) {
      const reports = (manifest.reports || [])
          .filter(r => r.platform !== 'app')
          .filter(r => (r.client_slug || '').toLowerCase() === slug.toLowerCase())
          .sort((a, b) => (b.date || '').localeCompare(a.date || ''));
      const latest = reports[0];
      if (!latest) return '<span class="u-badge cw-sin-reporte">Sin Cowork</span>';
      const v = (latest.verdict || '').toUpperCase().replace(/ /g, '_');
      const cls = v === 'LISTO' ? 'cw-listo'
          : v === 'CON_CONDICIONES' ? 'cw-condiciones'
          : (v === 'BLOQUEADO' || v === 'NO_APTO') ? 'cw-bloqueado'
          : 'cw-sin-reporte';
      const labelMap = { LISTO: 'LISTO', CON_CONDICIONES: 'CON CONDICIONES', BLOQUEADO: 'BLOQUEADO', NO_APTO: 'NO APTO' };
      const label = labelMap[v] || (latest.verdict ? escapeHtml(latest.verdict) : 'Sin Cowork');
      const stale = staleSuffixHtml(runDate, latest.date);
      return `<span class="u-badge ${cls}">${label}${stale}</span>`;
  }

  function renderMaestroBadge(slug, runDate, manifest) {
      if (!appClients.includes(slug.toLowerCase())) {
          return '<span class="u-badge u-na">N/A</span>';
      }
      const reports = (manifest.reports || [])
          .filter(r => r.platform === 'app')
          .filter(r => (r.client_slug || '').toLowerCase() === slug.toLowerCase())
          .sort((a, b) => (b.date || '').localeCompare(a.date || ''));
      const latest = reports[0];
      const health = latest?.health ?? latest?.score;  // backward compat Pitfall 5
      if (!latest || health == null) {
          return '<span class="u-badge u-na">N/A</span>';
      }
      const cls = health >= 80 ? 'mt-ok' : health >= 60 ? 'mt-warn' : 'mt-fail';
      const stale = staleSuffixHtml(runDate, latest.date);
      return `<span class="u-badge ${cls}">${health}/100${stale}</span>`;
  }

  async function updateUnifiedQaTable(run = latestRun) {
      const tbody = document.getElementById('unifiedQaBody');
      if (!tbody) return;
      if (!run || !run.clients || Object.keys(run.clients).length === 0) {
          tbody.innerHTML = '<tr><td colspan="4" class="loading">Sin datos en este run. Ejecuta /run-playwright para poblar la tabla.</td></tr>';
          resetUnifiedFilterPills();
          return;
      }
      const manifest = await loadManifestCached();
      const rows = Object.entries(run.clients)
          .sort(([aSlug, a], [bSlug, b]) => (a.name || aSlug).localeCompare(b.name || bSlug))  // D-13
          .map(([slug, c]) => {
              const pwBadge = renderPlaywrightBadge(c, run.date);
              const cwBadge = renderCoworkBadge(slug, run.date, manifest);
              const mtBadge = renderMaestroBadge(slug, run.date, manifest);
              const combined = pwBadge + cwBadge + mtBadge;
              const hasProblem = /pw-fail|cw-bloqueado|mt-fail/.test(combined);
              const hasStale = /u-badge-stale-suffix/.test(combined);
              const clientName = escapeHtml(c.name || slug);
              const clientSlug = escapeHtml(slug);
              return `<tr data-problemas="${hasProblem}" data-stale="${hasStale}">
                  <td><div class="u-client-name">${clientName}</div>
                      <div class="u-client-slug">${clientSlug}</div></td>
                  <td>${pwBadge}</td>
                  <td>${cwBadge}</td>
                  <td>${mtBadge}</td>
              </tr>`;
          });
      tbody.innerHTML = rows.join('') || '<tr><td colspan="4" class="loading">Sin datos en este run. Ejecuta /run-playwright para poblar la tabla.</td></tr>';
      resetUnifiedFilterPills();  // D-12: cambiar run resetea filtro a "Todos"
  }
  ```

  **Paso 2 — Agregar pill setup y reset functions (mismo bloque, después de `updateUnifiedQaTable`):**

  ```javascript
  function setupUnifiedFilterPills() {
      const container = document.getElementById('unifiedFilterPills');
      if (!container || container.dataset.wired === 'true') return;
      container.dataset.wired = 'true';
      container.addEventListener('click', (e) => {
          const pill = e.target.closest('.unified-filter-pill');
          if (!pill) return;
          container.querySelectorAll('.unified-filter-pill').forEach(p => p.classList.remove('active'));
          pill.classList.add('active');
          const tbody = document.getElementById('unifiedQaBody');
          if (!tbody) return;
          tbody.classList.remove('filter-problemas', 'filter-stale');
          const mode = pill.dataset.filter;
          if (mode === 'problemas') tbody.classList.add('filter-problemas');
          else if (mode === 'stale') tbody.classList.add('filter-stale');
      });
  }

  function resetUnifiedFilterPills() {
      const container = document.getElementById('unifiedFilterPills');
      if (!container) return;
      container.querySelectorAll('.unified-filter-pill').forEach(p => p.classList.remove('active'));
      const allPill = container.querySelector('[data-filter="all"]');
      if (allPill) allPill.classList.add('active');
      const tbody = document.getElementById('unifiedQaBody');
      if (tbody) tbody.classList.remove('filter-problemas', 'filter-stale');
  }
  ```

  **Notas sobre decisiones Claude's Discretion:**
  - `daysDiff(fromDate, toDate)` se extrae como helper compartido — tiene 3 call-sites (Playwright, Cowork, Maestro badges) vía `staleSuffixHtml`. Plan 03 también podrá reusar este helper si lo necesita en trend.
  - Event delegation sobre `#unifiedFilterPills` (no listener por pill) — más simple, menos memoria, consistente con el tamaño del codebase.
  - El flag `container.dataset.wired === 'true'` previene double-wire en caso de que `setupUnifiedFilterPills()` se llame dos veces (defensa).
  - `escapeHtml` se aplica a `c.name` y `slug` (Security Domain del RESEARCH — XSS mitigation explícita).
  - `.toUpperCase()` en verdict antes del replace garantiza consistencia con variantes de casing ("listo" vs "LISTO").
  - Pitfall 4 defense: Cowork filter usa `platform !== 'app'` en vez de `platform === 'b2b'` para no excluir entradas sin platform (manifest legacy).
  - Pitfall 5 defense: Maestro lee `rep.health ?? rep.score` para backward-compat con manifests pre-PIPE-01.
  </action>
  <verify>
    <automated>
    grep -c "function updateUnifiedQaTable" public/index.html    # == 1
    grep -c "function renderPlaywrightBadge" public/index.html   # == 1
    grep -c "function renderCoworkBadge" public/index.html       # == 1
    grep -c "function renderMaestroBadge" public/index.html      # == 1
    grep -c "function daysDiff" public/index.html                # == 1
    grep -c "function staleSuffixHtml" public/index.html         # == 1
    grep -c "function setupUnifiedFilterPills" public/index.html # == 1
    grep -c "function resetUnifiedFilterPills" public/index.html # == 1
    grep -c "function loadManifestCached" public/index.html      # == 1
    grep -c "const appClients" public/index.html                 # == 1
    grep -c "let cachedManifest" public/index.html               # == 1
    # JS parse check
    node --check <(sed -n '/<script>/,/<\/script>/p' public/index.html | sed '1d;$d') 2>&1 || python3 -c "
    import re
    with open('public/index.html') as f:
        html = f.read()
    scripts = re.findall(r'<script>(.*?)</script>', html, re.DOTALL)
    print(f'Found {len(scripts)} inline scripts; total chars: {sum(len(s) for s in scripts)}')
    "
    # Visual smoke test
    open public/index.html
    # En browser console (manual):
    # > typeof updateUnifiedQaTable === 'function'  -> true
    # > typeof daysDiff === 'function' -> true
    # > daysDiff('2026-04-19', '2026-04-17') -> 2
    # > daysDiff('2026-04-19', '2026-04-15') -> 4
    </automated>
  </verify>
  <acceptance_criteria>
    - [x] `const appClients = ['prinorte', 'surtiventas', 'coexito'];` declarado exactamente una vez, después de `let allRuns = [];`.
    - [x] `let cachedManifest = null;` declarado en el mismo bloque.
    - [x] `function loadManifestCached()` usa `fetch('manifest.json?v=' + Date.now())`, cachea en `cachedManifest`, retorna `{ reports: [] }` en caso de error.
    - [x] `function daysDiff(fromDateISO, toDateISO)` retorna `Math.round((new Date(fromDateISO) - new Date(toDateISO)) / 86400000)` o `null` si algún arg es falsy.
    - [x] `function staleSuffixHtml(runDate, sourceDate)` retorna string vacío si `daysDiff <= 2`; retorna `<span class="u-badge-stale-suffix">Hace N días</span>` cuando > 2. Singular "Hace 1 día" cuando `d === 1`.
    - [x] `function renderPlaywrightBadge(c, runDate)` retorna `<span class="u-badge u-na">N/A</span>` cuando `!c || !c.tests`; retorna `<span class="u-badge ${cls}">${pct}%${stale}</span>` con `cls` = pw-ok (100%) / pw-warn (70-99%) / pw-fail (<70%).
    - [x] `function renderCoworkBadge(slug, runDate, manifest)` filtra `platform !== 'app'`, match `client_slug.toLowerCase() === slug.toLowerCase()`, sort DESC por date, toma `[0]`; si no hay match retorna `Sin Cowork`; label "CON CONDICIONES" para CON_CONDICIONES.
    - [x] `function renderMaestroBadge(slug, runDate, manifest)` retorna N/A si `slug` no está en `appClients`; lee `latest.health ?? latest.score`; cls mt-ok (≥80) / mt-warn (60-79) / mt-fail (<60); label `${health}/100`.
    - [x] `function updateUnifiedQaTable(run = latestRun)` orquesta: guard null run, load manifest cached, Object.entries sort por `c.name`, map → 3 badges → `<tr data-problemas data-stale>`, `tbody.innerHTML = rows.join('')`, llama `resetUnifiedFilterPills()` al final.
    - [x] `function setupUnifiedFilterPills()` usa event delegation sobre `#unifiedFilterPills`, radio behavior (remove active en todos, add en clicked), aplica class `filter-problemas|filter-stale` a `#unifiedQaBody`.
    - [x] `function resetUnifiedFilterPills()` remove `active` de todos los pills, add `active` a `[data-filter="all"]`, remove `filter-problemas|filter-stale` de tbody.
    - [x] `escapeHtml(c.name)` y `escapeHtml(slug)` aplicados en el template literal de cada `<tr>`.
    - [x] Parse check: ninguna línea JS nueva introduce errores de sintaxis — el browser no reporta errores al cargar.
    - [x] En browser console: `typeof updateUnifiedQaTable === 'function'` → true; `daysDiff('2026-04-19', '2026-04-17')` → 2.
  </acceptance_criteria>
  <done>
    Helpers, 3 badge renderers, orchestrator, y filter pill logic definidos como funciones declaradas (no expresadas), todas accesibles desde el scope global del script. Ninguna está invocada todavía — Task 2 las cablea en `initDashboard` y en el listener adicional de `#runSelector`. El dashboard en browser NO debe mostrar cambios visibles aún (solo nuevas funciones en memoria).
  </done>
</task>

<task type="auto">
  <name>Task 2: Invocar desde initDashboard + listener adicional sobre #runSelector (D-01)</name>
  <files>public/index.html</files>
  <read_first>
    - public/index.html líneas 1481-1503 (initDashboard)
    - public/index.html líneas 1945-1967 (populateRunSelector + existing change listener de Phase 2 — NO MODIFICAR)
    - .planning/phases/03-unified-qa-status-view/03-CONTEXT.md (D-01, D-02, D-12)
    - .planning/phases/03-unified-qa-status-view/03-RESEARCH.md (Pattern 2: Multiple Independent Listeners, Pitfall 6)
    - .planning/phases/02-data-freshness-signals/02-02-SUMMARY.md (Task 2 — patrón de change listener de Phase 2 para referencia)
  </read_first>
  <action>
  **Paso 1 — Modificar `initDashboard()` (líneas 1481-1503). Estado actual relevante:**

  ```javascript
  async function initDashboard() {
      try {
          allRuns = await loadHistoryIndex();
          if (!allRuns.length) {
              ...
          }
          latestRun = await loadRunDetails(allRuns[0].date);
          if (!latestRun) return;
          ...
          updateClients();             // línea ~1495
          populateRunSelector();       // línea ~1496
          updateTrendChart();          // línea ~1497
          ...
      }
  }
  ```

  **Agregar tres líneas nuevas INMEDIATAMENTE DESPUÉS de `populateRunSelector();` (línea 1496) y ANTES de `updateTrendChart();`:**

  ```javascript
          updateClients();
          populateRunSelector();
          setupUnifiedFilterPills();                    // Phase 3: wire pills una vez
          updateUnifiedQaTable(latestRun);              // Phase 3: render inicial (D-02)
          wireUnifiedQaRunListener();                   // Phase 3: listener adicional sobre #runSelector
          updateTrendChart();
  ```

  **Paso 2 — Agregar función `wireUnifiedQaRunListener()` en el bloque `<script>`, ubicada entre `populateRunSelector()` (termina línea ~1967) y `function toggleCard` (línea ~1969).**

  ```javascript
  function wireUnifiedQaRunListener() {
      const sel = document.getElementById('runSelector');
      if (!sel || sel.dataset.phase3Wired === 'true') return;
      sel.dataset.phase3Wired = 'true';
      // D-01: listener ADICIONAL — no reemplaza el de Phase 2. Ambos se disparan en orden de registro.
      sel.addEventListener('change', (e) => {
          loadRunDetails(e.target.value).then(run => {
              if (run) {
                  updateUnifiedQaTable(run);
                  // resetUnifiedFilterPills ya se invoca dentro de updateUnifiedQaTable (D-12)
              }
          });
      });
  }
  ```

  **Notas críticas:**
  - NO modificar el listener existente en `populateRunSelector` (líneas 1958-1965 de Phase 2). D-01 es explícito: dos listeners independientes.
  - `setupUnifiedFilterPills()` se llama ANTES del primer `updateUnifiedQaTable(latestRun)` para cumplir Pitfall 6 (listener registrado antes del render garantiza clicks tempranos funcionen; aunque la tabla renderiza después, la clase `filter-*` se aplica a `#unifiedQaBody` independiente del contenido).
  - El flag `sel.dataset.phase3Wired === 'true'` previene doble-wire si `initDashboard()` se re-ejecuta (defensa consistente con `setupUnifiedFilterPills`).
  - Aceptar el double-fetch de `loadRunDetails(date)` por `#runSelector change` (Pitfall 2 RESOLVED en RESEARCH — dos fetches paralelos del mismo URL, browser HTTP cache mitiga). Documentar en el SUMMARY.
  - Phase 2 listener llama `updateClients(run)`; Phase 3 listener llama `updateUnifiedQaTable(run)`. Ambos operan sobre el mismo `run` resuelto pero desde dos promesas separadas — orden de resolución indeterminado pero irrelevante porque tocan tbodies distintos (clientsContainer vs unifiedQaBody).
  </action>
  <verify>
    <automated>
    grep -c "setupUnifiedFilterPills();" public/index.html   # >= 2 (declaration + invocation en initDashboard) — note: declaration line uses `function setupUnifiedFilterPills()` without semicolon, so this grep catches only the invocation. Use alt:
    grep -c "setupUnifiedFilterPills()" public/index.html    # >= 2
    grep -c "updateUnifiedQaTable(latestRun);" public/index.html  # == 1
    grep -c "wireUnifiedQaRunListener();" public/index.html  # == 1
    grep -c "function wireUnifiedQaRunListener" public/index.html # == 1
    # Verificar que el listener Phase 2 NO fue modificado
    sed -n '1955,1970p' public/index.html | grep -c "updateClients(run)"  # == 1 (intacto)
    sed -n '1955,1970p' public/index.html | grep -c "updateUnifiedQaTable"  # == 0 (no debe estar en populateRunSelector)
    # Visual smoke test
    open public/index.html
    # En browser:
    # 1. Al cargar: tabla "Estado QA por Cliente" muestra filas con badges reales, 3 pills visibles (Todos activo)
    # 2. Click pill "Con problemas": filas sin fail se ocultan
    # 3. Click pill "Stale": filas sin sufijo ámbar se ocultan
    # 4. Click pill "Todos": todas las filas vuelven a aparecer
    # 5. Cambiar #runSelector: tabla se re-renderiza Y pill resetea a "Todos"
    # 6. Phase 2 sigue funcionando: cards B2B re-renderizan al cambiar run
    </automated>
  </verify>
  <acceptance_criteria>
    - [x] `initDashboard()` invoca (en este orden): `updateClients();` → `populateRunSelector();` → `setupUnifiedFilterPills();` → `updateUnifiedQaTable(latestRun);` → `wireUnifiedQaRunListener();` → `updateTrendChart();`. Verificable con `grep -A 10 "async function initDashboard"`.
    - [x] `function wireUnifiedQaRunListener()` existe, usa event delegation sobre `#runSelector`, flag `dataset.phase3Wired` previene doble-wire.
    - [x] Listener de Phase 2 en `populateRunSelector()` (líneas originales 1958-1965) SIN MODIFICAR — `sed -n '1955,1970p'` sigue mostrando `loadRunDetails(e.target.value).then(run => { if (run) { updateClients(run); } else { console.warn(...); } });`.
    - [x] Ambos listeners coexisten: al cambiar `#runSelector`, `updateClients` Y `updateUnifiedQaTable` ambos re-renderizan.
    - [x] Al cambiar `#runSelector`, el filter pill se resetea a "Todos" (vía `resetUnifiedFilterPills` invocado dentro de `updateUnifiedQaTable`).
    - [x] En browser console al cargar: `document.getElementById('unifiedQaBody').querySelectorAll('tr').length > 0` (filas renderizadas).
    - [x] En browser console: `document.querySelector('.unified-filter-pill.active').dataset.filter === 'all'` al cargar.
    - [x] Al clickear `[data-filter="problemas"]`: las filas sin atributo `data-problemas="true"` se ocultan (display none).
    - [x] Al clickear `[data-filter="stale"]`: las filas sin atributo `data-stale="true"` se ocultan.
    - [x] Tab APP, b2bVars card, Cowork reports grid, client card grid de Phase 2, trend chart — TODOS siguen renderizando sin cambios visibles.
  </acceptance_criteria>
  <done>
    La tabla "Estado QA por Cliente" renderiza al cargar con los 3 badges por cliente (Playwright + Cowork + Maestro con sufijo stale cuando aplica). Los filter pills funcionan sin recargar. Cambiar de run re-renderiza la tabla y resetea el pill. Phase 2 y todo el resto del dashboard siguen funcionando idéntico. DASH-03 y DASH-04 están funcionalmente completos — falta solo DASH-05 (Plan 03).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Pages CDN → Browser | JSON de `history/*.json` y `manifest.json` se fetchean desde same-origin — no hay cross-origin, no hay input de usuario atravesando una red. |
| JSON files → DOM via innerHTML | `c.name`, `slug`, `latest.verdict` vienen de JSON que produce el pipeline interno. El repo es privado y solo YOM escribe ahí. Riesgo XSS teórico si alguien inyecta HTML en un client name — mitigado con `escapeHtml`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering (XSS) | `c.name` / `slug` interpolados en `tbody.innerHTML` via template literal | mitigate | `escapeHtml(c.name || slug)` y `escapeHtml(slug)` aplicados explícitamente en el `<tr>` template. `verdict` también se pasa por `escapeHtml` en el fallback, aunque el labelMap cubre los valores esperados. |
| T-03-04 | Tampering (XSS) | `rep.client_slug` en filter logic | accept | Se usa solo para comparación `.toLowerCase()` — nunca se interpola en DOM. |
| T-03-05 | Denial of service | Manifest con miles de reports → filter O(N) por cliente × 17 clientes × 3 badges | accept | Manifest actual tiene <10 reports. Array.filter O(N*17) = <170 ops. Si crece a miles, considerar indexar por slug en un Map (deferred). |
| T-03-06 | Information disclosure | Logs en console de `loadRunDetails` en caso de error | accept | Dashboard es interno, console logs no exponen secretos. |
</threat_model>

<verification>
1. **Sintaxis válida:** El browser carga `public/index.html` sin errores en console.
2. **Funciones declaradas:** `typeof updateUnifiedQaTable === 'function'` etc. en browser console.
3. **Data flowing:** Tabla muestra ≥1 fila con 3 badges (pw + cw + mt) para cada cliente en `selectedRun.clients`.
4. **Badge variants correctos:** Según el `selectedRun` del día, validar visualmente:
   - Cliente con 100% passed tiene badge verde `100%`.
   - Cliente con pass% 70-99 tiene badge ámbar.
   - Cliente con pass% <70 tiene badge rojo.
   - Cliente sin tests tiene badge N/A (neutro).
   - Cliente en appClients con health ≥80 tiene badge verde `{N}/100`.
   - Cliente fuera de appClients tiene Maestro badge = N/A.
   - Cliente con verdict Cowork "CON CONDICIONES" tiene pill ámbar con texto "CON CONDICIONES".
5. **Stale suffix visible:** Si algún badge tiene source date >2 días vs selectedRun.date, aparece `· Hace N días` inline.
6. **Filter pills funcionales:**
   - "Todos" muestra todas las filas.
   - "Con problemas" oculta filas sin ningún pw-fail/cw-bloqueado/mt-fail.
   - "Stale" oculta filas sin ningún sufijo ámbar.
   - Solo un pill activo a la vez.
7. **Run selector integration:** Cambiar `#runSelector` re-renderiza la tabla Y resetea pill a "Todos" sin recargar.
8. **Backward compat:** Phase 2 (freshness badges en client cards), tab APP, Cowork reports grid, trend chart — TODOS idénticos a antes de Phase 3.
</verification>

<success_criteria>
- [x] DASH-03 satisfecho: sección "Estado QA por Cliente" muestra una fila por cliente × 3 badges (Playwright pass% / Cowork verdict / Maestro health o N/A).
- [x] DASH-04 satisfecho: cada badge muestra sufijo ámbar "Hace N días" cuando su source date es >2 días respecto a `selectedRun.date`.
- [x] Filter pills (D-08 a D-12) funcionan: Todos/Con problemas/Stale con radio behavior, reset al cambiar run.
- [x] Integración D-01: dos listeners independientes sobre `#runSelector` (Phase 2 intacto + Phase 3 nuevo).
- [x] Orden D-13: filas ordenadas alfabéticamente por `c.name`.
- [x] Universo D-15: solo clientes de `selectedRun.clients` aparecen (no Cowork-only clients).
- [x] No regresión: B2B cards, APP tab, Cowork grid, trend chart, donut, summary row siguen idénticos.
- [x] Manifest cached: `loadManifestCached` se llama múltiples veces pero solo hace 1 fetch por sesión.
- [x] XSS-safe: `escapeHtml` aplicado a `c.name` y `slug`.
</success_criteria>

<output>
After completion, create `.planning/phases/03-unified-qa-status-view/03-02-SUMMARY.md` describing:
- Funciones JS agregadas (listado con ubicación de línea post-edit)
- Constante `appClients` y cache `cachedManifest` agregados
- Invocaciones en `initDashboard` (orden exacto)
- Nueva función `wireUnifiedQaRunListener` y cómo coexiste con el listener de Phase 2 (D-01)
- Defensas aplicadas: Pitfall 3 (Math.round), Pitfall 4 (platform filter), Pitfall 5 (health ?? score), Pitfall 6 (setup antes de render), XSS via escapeHtml
- Verificación del double-fetch aceptado (browser HTTP cache)
- Matriz de badges verificada visualmente contra `selectedRun` del día
</output>
