---
phase: 03-unified-qa-status-view
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/index.html
autonomous: true
requirements:
  - DASH-03
  - DASH-04
user_setup: []

must_haves:
  truths:
    - "CSS block for unified table (.unified-subtitle, .unified-table-wrapper, .unified-qa-table, .u-col-client, .u-col-badge, .u-client-name, .u-client-slug) exists in <style>"
    - "CSS block for badges (.u-badge base, .u-badge-stale-suffix, .u-badge.pw-ok/pw-warn/pw-fail, .u-badge.cw-listo/cw-condiciones/cw-bloqueado/cw-sin-reporte, .u-badge.mt-ok/mt-warn/mt-fail, .u-badge.u-na) exists"
    - "CSS for filter pills (.unified-filter-pills, .unified-filter-pill, .unified-filter-pill.active, hover state, filter-problemas/filter-stale tbody rules) exists"
    - "CSS for trend header (.trend-header, .trend-client-nav) exists"
    - "HTML card 'Estado QA por Cliente' inserted between coworkReportsGrid and trend card"
    - "Filter pills container (#unifiedFilterPills) with 3 buttons (Todos active, Con problemas, Stale) rendered above table"
    - "Trend card wrapped with .trend-header containing card-title + #trendClientSelector select"
  artifacts:
    - path: "public/index.html"
      provides: "CSS tokens + HTML skeleton for unified QA section + filter pills + trend selector container"
      contains_css: [".unified-qa-table", ".u-badge", ".u-badge-stale-suffix", ".u-badge.pw-ok", ".u-badge.pw-warn", ".u-badge.pw-fail", ".u-badge.cw-listo", ".u-badge.cw-condiciones", ".u-badge.cw-bloqueado", ".u-badge.cw-sin-reporte", ".u-badge.mt-ok", ".u-badge.mt-warn", ".u-badge.mt-fail", ".u-badge.u-na", ".unified-filter-pill", ".unified-filter-pill.active", ".trend-header", ".trend-client-nav"]
      contains_html: ["id=\"unifiedQaBody\"", "id=\"unifiedFilterPills\"", "id=\"trendClientSelector\""]
  key_links:
    - from: "<style> block"
      to: "table/pills/trend-header classes"
      via: "CSS rule definitions"
      pattern: ".unified-qa-table|.u-badge|.unified-filter-pill|.trend-header"
    - from: "coworkReportsGrid card close tag"
      to: "trend card open tag"
      via: "New <div class=\"card\"> block inserted in between"
      pattern: "Estado QA por Cliente"
    - from: "trend card"
      to: ".trend-header wrapper"
      via: "Existing card-title + new .trend-client-nav siblings"
      pattern: "trendClientSelector"
---

<objective>
Agregar toda la base estática (CSS + HTML skeleton) para la nueva sección "Estado QA por Cliente", los filter pills, y el wrap del header del trend chart. Plan puramente aditivo sobre `public/index.html` — sin JavaScript todavía. El `<tbody id="unifiedQaBody">` queda con un loading row; el `<select id="trendClientSelector">` queda vacío. Plan 02 y Plan 03 cablearán la data.

Purpose: Separar el boilerplate visual del comportamiento. Este plan no puede romper nada porque no cambia ningún código existente — solo inyecta CSS y HTML nuevos. Valida que el UI-SPEC aprobado se reproduzca en el DOM antes de agregar complejidad de renderización y Chart.js.

Output: `public/index.html` modificado con ~160 líneas CSS nuevas y ~25 líneas HTML nuevas. Cero cambios en lógica existente.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-unified-qa-status-view/03-CONTEXT.md
@.planning/phases/03-unified-qa-status-view/03-RESEARCH.md
@.planning/phases/03-unified-qa-status-view/03-UI-SPEC.md
@.planning/phases/02-data-freshness-signals/02-UI-SPEC.md
@public/index.html

<interfaces>
<!-- Líneas clave verificadas en public/index.html -->

Existing CSS tokens relevant to Phase 3 (already in <style>):
```
Line 579-592: .freshness-badge, .freshness-badge.freshness-stale (tokens #fef3c7/#92400e)
Line 593-615: .run-nav, .run-nav-label, .run-select, .run-select:focus (accent #667eea)
Line 737-739: .verdict-listo, .verdict-condiciones, .verdict-bloqueado (reference shapes — Phase 3 DUPLICATES these into .u-badge.cw-* per UI-SPEC Open Question 3 RESOLVED)
```

Existing DOM insertion anchors:
```
Line 1358-1364: <div class="card"> Cowork reports grid — CLOSING </div> at 1364
Line 1366-1383: <div class="card"> Trend chart (📈 Tendencia Histórica) — must be wrapped with .trend-header
Line 1385: </div><!-- /tab-b2b -->
```

Additive rule (CRITICAL):
- NO modifications to lines 1358-1364 (Cowork grid card) OR lines 1369-1383 (chart-grid, canvases, donut)
- The ONLY edit to the trend card (lines 1366-1383) is wrapping <div class="card-title">📈 Tendencia Histórica</div> in a new <div class="trend-header">...</div> container that also holds the .trend-client-nav
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CSS tokens + HTML skeleton for unified table, filter pills, and trend header wrap</name>
  <files>public/index.html</files>
  <read_first>
    - public/index.html (lectura completa — archivo único de 2690 líneas; especialmente líneas 575-760 del <style> y líneas 1355-1385 del tab-b2b)
    - .planning/phases/03-unified-qa-status-view/03-UI-SPEC.md (secciones Color, Typography, Component Inventory 1-4, Notas de implementación items 1-3)
    - .planning/phases/03-unified-qa-status-view/03-CONTEXT.md (decisiones D-08 a D-12 sobre filter pills)
    - .planning/phases/03-unified-qa-status-view/03-RESEARCH.md (Pattern 4 Approach A para filter CSS)
  </read_first>
  <action>
  **Paso 1 — Insertar CSS después de la línea 615 (`.run-select:focus { outline: none; border-color: #667eea; }`), antes de la línea 616 (`.pass-rate-badge {`).**

  Bloque CSS exacto a insertar (160 líneas). Agrupar con un comentario separador `/* ── Unified QA Status View (Phase 3) ── */`:

  ```css
        /* ── Unified QA Status View (Phase 3) ── */
        .unified-subtitle {
            color: #6b7280;
            font-size: 13px;
            margin: 0 0 16px;
        }
        .unified-table-wrapper {
            overflow-x: auto;
            border: 1px solid #e5e7eb;
            border-radius: 8px;
        }
        .unified-qa-table {
            width: 100%;
            border-collapse: collapse;
            font-size: 14px;
        }
        .unified-qa-table thead { background: #f9fafb; }
        .unified-qa-table th {
            text-align: left;
            padding: 8px 16px;
            font-size: 11px;
            font-weight: 700;
            color: #6b7280;
            text-transform: uppercase;
            letter-spacing: 0.04em;
            border-bottom: 1px solid #e5e7eb;
        }
        .unified-qa-table td {
            padding: 12px 16px;
            border-bottom: 1px solid #f3f4f6;
            vertical-align: middle;
        }
        .unified-qa-table tbody tr:last-child td { border-bottom: none; }
        .unified-qa-table tbody tr:hover { background: #f9fafb; }
        .u-col-client { width: 30%; }
        .u-col-badge { width: 23.33%; }
        .u-client-name {
            font-weight: 700;
            color: #111827;
            font-size: 14px;
        }
        .u-client-slug {
            font-size: 11px;
            color: #9ca3af;
            font-weight: 400;
            margin-top: 2px;
        }

        /* Badges (Playwright / Cowork / Maestro) */
        .u-badge {
            display: inline-flex;
            align-items: center;
            gap: 4px;
            padding: 4px 10px;
            border-radius: 9999px;
            font-size: 11px;
            font-weight: 700;
            white-space: nowrap;
            line-height: 1.2;
        }
        .u-badge-stale-suffix {
            font-size: 11px;
            font-weight: 600;
            color: #92400e;
            background: #fef3c7;
            padding: 2px 6px;
            border-radius: 9999px;
            margin-left: 6px;
        }
        .u-badge.pw-ok        { background: #d1fae5; color: #065f46; }
        .u-badge.pw-warn      { background: #fef3c7; color: #92400e; }
        .u-badge.pw-fail      { background: #fee2e2; color: #991b1b; }
        .u-badge.cw-listo       { background: #d1fae5; color: #065f46; }
        .u-badge.cw-condiciones { background: #fef3c7; color: #92400e; }
        .u-badge.cw-bloqueado   { background: #fee2e2; color: #991b1b; }
        .u-badge.cw-sin-reporte { background: #f3f4f6; color: #9ca3af; border: 1px solid #e5e7eb; }
        .u-badge.mt-ok        { background: #d1fae5; color: #065f46; }
        .u-badge.mt-warn      { background: #fef3c7; color: #92400e; }
        .u-badge.mt-fail      { background: #fee2e2; color: #991b1b; }
        .u-badge.u-na         { background: #f3f4f6; color: #9ca3af; border: 1px solid #e5e7eb; }

        /* Filter pills (D-08 a D-12) */
        .unified-filter-pills {
            display: flex;
            gap: 8px;
            margin: 0 0 16px;
            flex-wrap: wrap;
        }
        .unified-filter-pill {
            padding: 6px 14px;
            border-radius: 9999px;
            border: none;
            background: #f3f4f6;
            color: #6b7280;
            font-size: 12px;
            font-weight: 600;
            cursor: pointer;
            font-family: inherit;
            transition: background 0.15s, color 0.15s;
        }
        .unified-filter-pill:hover { background: #e5e7eb; }
        .unified-filter-pill.active {
            background: #667eea;
            color: #ffffff;
        }
        /* Client-side filter toggle via class on tbody (Pattern 4 Approach A) */
        #unifiedQaBody.filter-problemas tr:not([data-problemas="true"]) { display: none; }
        #unifiedQaBody.filter-stale     tr:not([data-stale="true"])    { display: none; }

        /* Trend header (wraps card-title + client selector) */
        .trend-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 16px;
            flex-wrap: wrap;
            gap: 8px;
        }
        .trend-client-nav {
            display: flex;
            align-items: center;
            gap: 8px;
        }
  ```

  **Paso 2 — Insertar HTML del card "Estado QA por Cliente" entre la línea 1364 (`</div>` que cierra el card Cowork) y la línea 1366 (`<!-- Trend chart -->`).**

  Reemplazar exactamente las líneas 1364-1366. Estado actual:
  ```
  1364:        </div>
  1365: (línea en blanco)
  1366:        <!-- Trend chart -->
  ```

  Estado después de la edición:
  ```
  1364:        </div>
  1365: (línea en blanco)
  NUEVO:        <!-- Unified QA Status per Client (Phase 3) -->
  NUEVO:        <div class="card">
  NUEVO:            <div class="card-title">🎯 Estado QA por Cliente</div>
  NUEVO:            <p class="unified-subtitle">Resumen de las tres pipelines por cliente. N/A = pipeline no aplica a este cliente.</p>
  NUEVO:            <div class="unified-filter-pills" id="unifiedFilterPills" role="tablist">
  NUEVO:                <button class="unified-filter-pill active" data-filter="all" type="button">Todos</button>
  NUEVO:                <button class="unified-filter-pill" data-filter="problemas" type="button">Con problemas</button>
  NUEVO:                <button class="unified-filter-pill" data-filter="stale" type="button">Stale</button>
  NUEVO:            </div>
  NUEVO:            <div class="unified-table-wrapper">
  NUEVO:                <table class="unified-qa-table">
  NUEVO:                    <thead>
  NUEVO:                        <tr>
  NUEVO:                            <th class="u-col-client">Cliente</th>
  NUEVO:                            <th class="u-col-badge">Playwright</th>
  NUEVO:                            <th class="u-col-badge">Cowork</th>
  NUEVO:                            <th class="u-col-badge">Maestro</th>
  NUEVO:                        </tr>
  NUEVO:                    </thead>
  NUEVO:                    <tbody id="unifiedQaBody">
  NUEVO:                        <tr><td colspan="4" class="loading">Cargando estado unificado…</td></tr>
  NUEVO:                    </tbody>
  NUEVO:                </table>
  NUEVO:            </div>
  NUEVO:        </div>
  NUEVO: (línea en blanco)
  (siguiente línea preservada): <!-- Trend chart -->
  ```

  **Paso 3 — Refactor del header del trend card (líneas 1366-1368).**

  Estado actual (líneas 1366-1368):
  ```
  1366:        <!-- Trend chart -->
  1367:        <div class="card">
  1368:            <div class="card-title">📈 Tendencia Histórica</div>
  ```

  Estado después (envolver card-title con .trend-header + agregar client selector):
  ```
          <!-- Trend chart -->
          <div class="card">
              <div class="trend-header">
                  <div class="card-title">📈 Tendencia Histórica</div>
                  <div class="trend-client-nav">
                      <span class="run-nav-label">Cliente:</span>
                      <select id="trendClientSelector" class="run-select"></select>
                  </div>
              </div>
  ```

  **NO modificar las líneas 1369-1383** (el `<div class="chart-grid">` y todo su contenido con los dos canvases). Solo se agregan 7 líneas antes de `<div class="chart-grid">` y se quita 1 línea (el `<div class="card-title">📈 Tendencia Histórica</div>` original, ahora dentro del wrapper).

  **Reglas críticas:**
  - No tocar NADA fuera de las secciones indicadas (tab-app, client-card grid, reports-grid, summary row, failure analysis, pending B2B, b2bVars card, donut canvas).
  - El `<select id="trendClientSelector">` queda VACÍO — Plan 03 lo populará.
  - El `<tbody id="unifiedQaBody">` queda con la loading row — Plan 02 la reemplazará.
  - El pill `[data-filter="all"]` ya viene con clase `active` (default activo al cargar, D-12).
  - Ningún selector `document.getElement...` se agrega en este plan — es puramente estructura.

  **Justificación accent extendido (D-10):** El UI-SPEC Dimension 3 reserva `#667eea` para `:focus` de `.run-select` y la línea del trend filtrado. Este plan agrega un tercer uso: fondo del pill activo (`.unified-filter-pill.active`). Documentar en el commit message que esta es una extensión sancionada por D-10 de 03-CONTEXT.md, aceptando la complementariedad explícita sobre el UI-SPEC aprobado.
  </action>
  <verify>
    <automated>
    grep -c "\.unified-qa-table" public/index.html  # debe ser >= 5 (rules CSS)
    grep -c "\.u-badge" public/index.html          # debe ser >= 12 (base + variants)
    grep -c "\.unified-filter-pill" public/index.html # debe ser >= 4
    grep -c "\.trend-header" public/index.html     # debe ser >= 2
    grep -c "id=\"unifiedQaBody\"" public/index.html # debe ser == 1
    grep -c "id=\"unifiedFilterPills\"" public/index.html # debe ser == 1
    grep -c "id=\"trendClientSelector\"" public/index.html # debe ser == 1
    grep -c "🎯 Estado QA por Cliente" public/index.html # debe ser == 1
    grep -c "Cargando estado unificado" public/index.html # debe ser == 1
    grep -c "Todos los clientes (agregado)" public/index.html # debe ser == 0 (lo agregará Plan 03)
    # Sanidad: archivos y tamaño razonables
    wc -l public/index.html  # ~2690 líneas + ~185 nuevas = ~2875
    # Validar HTML parseable (Python one-liner)
    python3 -c "import html.parser; p=html.parser.HTMLParser(); p.feed(open('public/index.html').read()); print('HTML parse OK')"
    # Abrir en navegador y validar visualmente (manual una vez)
    open public/index.html
    </automated>
  </verify>
  <acceptance_criteria>
    - [x] CSS `.unified-qa-table { width: 100%; border-collapse: collapse; font-size: 14px; }` existe entre línea 615 (existente `.run-select:focus`) y la línea original 616 (ahora desplazada) que contiene `.pass-rate-badge {`.
    - [x] CSS `.u-badge { display: inline-flex; ... padding: 4px 10px; border-radius: 9999px; font-size: 11px; font-weight: 700; }` existe con esos valores exactos.
    - [x] CSS `.u-badge-stale-suffix { ... color: #92400e; background: #fef3c7; ... }` existe.
    - [x] CSS variants: `.u-badge.pw-ok`, `.u-badge.pw-warn`, `.u-badge.pw-fail`, `.u-badge.cw-listo`, `.u-badge.cw-condiciones`, `.u-badge.cw-bloqueado`, `.u-badge.cw-sin-reporte`, `.u-badge.mt-ok`, `.u-badge.mt-warn`, `.u-badge.mt-fail`, `.u-badge.u-na` — TODAS presentes con los colores del UI-SPEC.
    - [x] CSS `.unified-filter-pill.active { background: #667eea; color: #ffffff; }` existe.
    - [x] CSS `#unifiedQaBody.filter-problemas tr:not([data-problemas="true"]) { display: none; }` existe literal.
    - [x] CSS `#unifiedQaBody.filter-stale tr:not([data-stale="true"]) { display: none; }` existe literal.
    - [x] HTML card "🎯 Estado QA por Cliente" existe exactamente entre el `</div>` que cierra el card Cowork (post-línea 1364) y `<!-- Trend chart -->` (línea original 1366, ahora desplazada).
    - [x] HTML filter pills: 3 botones `<button class="unified-filter-pill..." data-filter="all|problemas|stale" type="button">Todos|Con problemas|Stale</button>` con el pill `all` teniendo clase `active`.
    - [x] HTML tabla con `<thead>` de 4 `<th>` (Cliente/Playwright/Cowork/Maestro) y `<tbody id="unifiedQaBody">` con una loading row `<tr><td colspan="4" class="loading">Cargando estado unificado…</td></tr>`.
    - [x] HTML `.trend-header` wrapea `<div class="card-title">📈 Tendencia Histórica</div>` + `<div class="trend-client-nav">` con `<span class="run-nav-label">Cliente:</span>` y `<select id="trendClientSelector" class="run-select"></select>` (select vacío).
    - [x] `grep -n "<div class=\"chart-grid\">" public/index.html` devuelve la línea con el chart-grid INTACTA (canvases no tocados).
    - [x] `grep -n "📋 Reportes QA Cowork" public/index.html` encuentra exactamente 1 match y la card Cowork no se modificó (line 1360 original o equivalente post-shift).
    - [x] `python3 -c "import html.parser; p=html.parser.HTMLParser(); p.feed(open('public/index.html').read())"` termina sin excepciones.
    - [x] Al abrir `public/index.html` en el browser: se ve el nuevo card "🎯 Estado QA por Cliente" con los 3 pills (Todos activo) y el loading row; el trend chart sigue renderizando igual; el selector de cliente aparece vacío al lado del título del trend.
  </acceptance_criteria>
  <done>
    `public/index.html` tiene ~185 líneas nuevas: ~160 de CSS (tokens de tabla + badges + pills + trend header) y ~25 de HTML (card nueva + wrap del trend header). Cero líneas existentes modificadas excepto el wrap local del `<div class="card-title">📈 Tendencia Histórica</div>`. El browser renderiza el skeleton sin errores JS (las funciones `updateUnifiedQaTable` y `populateTrendClientSelector` todavía no existen, pero nadie las llama todavía — Plan 02 y 03 las agregan juntas con sus invocaciones en `initDashboard`). El loading row permanece visible hasta que Plan 02 lo sobrescriba.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No hay nuevas trust boundaries. Este plan solo agrega CSS y HTML estático. No hay input de usuario, no hay fetch, no hay parsing de JSON, no hay ejecución de código.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information disclosure | CSS class names expuestas en HTML público | accept | Dashboard es repo privado YOM; nombres de clase no son secretos y siguen convención de Phase 2. |
| T-03-02 | Tampering (XSS) | HTML literal hardcoded sin input de usuario | accept | Todo el HTML en este plan es string literal en el archivo — no hay `innerHTML` dinámico con data externa. Plan 02 introducirá `escapeHtml` para los valores dinámicos. |
</threat_model>

<verification>
1. **HTML válido:** `python3 -c "import html.parser; p=html.parser.HTMLParser(); p.feed(open('public/index.html').read())"` retorna sin excepciones.
2. **CSS válido:** Abrir `public/index.html` en Chrome DevTools → Console → verificar 0 errores CSS.
3. **Regression visual:** Al abrir el dashboard, los siguientes componentes se ven IDÉNTICOS a antes:
   - Summary row (4 cards métricas)
   - Run selector (#runSelector) de Phase 2
   - B2B clients grid (Phase 2 freshness badges)
   - Cowork reports grid (coworkReportsGrid)
   - Trend chart (línea + donut)
   - Tab APP (no modificado)
4. **Skeleton visible:** Entre "Reportes QA Cowork" y "Tendencia Histórica" aparece el nuevo card "🎯 Estado QA por Cliente" con 3 pills (Todos activo) y el loading row.
5. **Trend header re-layout:** El título "📈 Tendencia Histórica" está ahora alineado a la izquierda con el selector de cliente vacío a la derecha (flex space-between).
</verification>

<success_criteria>
- [x] CSS foundations completas para unified table, badges (11 variantes), filter pills, y trend-header.
- [x] HTML skeleton de card "🎯 Estado QA por Cliente" insertado en el lugar correcto.
- [x] HTML skeleton de 3 filter pills insertado encima de la tabla.
- [x] Trend card header refactoreado con .trend-header wrapper + .trend-client-nav + `<select id="trendClientSelector">`.
- [x] Cero funcionalidad existente rota (B2B tab, APP tab, Cowork card, donut chart, run selector de Phase 2 siguen idénticos).
- [x] El `<select id="trendClientSelector">` y el `<tbody id="unifiedQaBody">` quedan con estado inicial (vacío / loading) listos para que Plan 02 y 03 los hidraten.
- [x] Contribuye a DASH-03 y DASH-04 proveyendo todas las clases CSS que Plan 02 necesita para aplicar las variantes de badge.
</success_criteria>

<output>
After completion, create `.planning/phases/03-unified-qa-status-view/03-01-SUMMARY.md` describing:
- CSS blocks agregados (tokens exactos con contextos de línea)
- HTML blocks insertados (posiciones antes/después)
- Listado de clases CSS creadas (enumerando las 11 variantes de `.u-badge.*`)
- Verificación de additive rule: qué componentes NO se tocaron
- Nota sobre la extensión del accent `#667eea` a `.unified-filter-pill.active` (justificada por D-10 del 03-CONTEXT.md)
</output>
