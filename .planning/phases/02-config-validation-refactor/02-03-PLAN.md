---
phase: 02-config-validation-refactor
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - tests/e2e/b2b/config-validation/cv-payments.spec.ts
  - tests/e2e/b2b/config-validation/cv-orders.spec.ts
  - tests/e2e/b2b/config-validation/cv-ui-features.spec.ts
autonomous: true
requirements:
  - REQ-03
  - REQ-04
must_haves:
  truths:
    - "cv-payments.spec.ts contains 11 tests: enablePayments, disablePayments, enableSellerDiscount, taxes.showSummary, includeTaxRateInPrices, enablePaymentsCollection, hasTransferPaymentType, discountTypes.enableOrderDiscountType, discountTypes.enableProductDiscountType, payment.enableNewPaymentModule, paymentsWithoutAccount"
    - "cv-orders.spec.ts contains 9 tests: ordersRequireAuthorization, ordersRequireVerification, enableOrderValidation, enableOrderApproval, hasVoucherPrinterEnabled, shareOrderNewDesign, enableInvoicesList, enablePaymentDocumentsB2B, disableCommerceEdit"
    - "cv-ui-features.spec.ts contains 12 tests: pendingDocuments, pointsEnabled, enableTask, enableCreditNotes, showMinOne, footerCustomContent, externalAccess, blockedClientAlert, useNewPromotions, enableBetaButtons, suggestions.hide, enableNewUI"
    - "Each file is under 300 lines"
    - "Each file uses SELECTORS constants instead of inline regex patterns"
  artifacts:
    - path: "tests/e2e/b2b/config-validation/cv-payments.spec.ts"
      provides: "11 payment and discount tests"
    - path: "tests/e2e/b2b/config-validation/cv-orders.spec.ts"
      provides: "9 order management tests"
    - path: "tests/e2e/b2b/config-validation/cv-ui-features.spec.ts"
      provides: "12 navigation and UI feature tests"
  key_links:
    - from: "tests/e2e/b2b/config-validation/cv-payments.spec.ts"
      to: "tests/e2e/b2b/config-validation/helpers.ts"
      via: "import { loginIfNeeded, addOneProductToCart } from './helpers'"
      pattern: "import.*addOneProductToCart.*from.*./helpers"
    - from: "tests/e2e/b2b/config-validation/cv-orders.spec.ts"
      to: "tests/e2e/b2b/config-validation/helpers.ts"
      via: "import { loginIfNeeded, addOneProductToCart } from './helpers'"
      pattern: "import.*loginIfNeeded.*from.*./helpers"
    - from: "tests/e2e/b2b/config-validation/cv-ui-features.spec.ts"
      to: "tests/e2e/b2b/config-validation/selectors.ts"
      via: "import { SELECTORS } from './selectors'"
      pattern: "import.*SELECTORS.*from.*./selectors"
---

<objective>
Create the remaining 3 of 6 split spec files: cv-payments.spec.ts (11 tests), cv-orders.spec.ts (9 tests), cv-ui-features.spec.ts (12 tests). Total: 32 tests migrated.

Purpose: REQ-03 requires splitting into per-feature files under 300 lines each. REQ-04 requires using SELECTORS constants. This plan runs in parallel with Plan 02.
Output: 3 independently runnable spec files in `tests/e2e/b2b/config-validation/`.
</objective>

<execution_context>
@/Users/lalojimenez/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lalojimenez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-config-validation-refactor/02-RESEARCH.md
@.planning/phases/02-config-validation-refactor/02-01-SUMMARY.md

@tests/e2e/b2b/config-validation.spec.ts
@tests/e2e/b2b/config-validation/helpers.ts
@tests/e2e/b2b/config-validation/selectors.ts

<interfaces>
<!-- From helpers.ts (created in Plan 01) -->
```typescript
export async function loginIfNeeded(page: Page, client: ClientConfig): Promise<void>;
export async function addOneProductToCart(page: Page, client: ClientConfig): Promise<void>;
```

<!-- From selectors.ts (created in Plan 01) — key constants for these files -->
```typescript
export const SELECTORS: {
  PAYMENTS_CLASS: string;        // '[class*="payment" i], [class*="pago" i]'
  PAYMENT_MODULE_SELECTOR: string;
  PAYMENT_COLLECTIONS_TEXT: RegExp;
  PAYMENT_COLLECTIONS_LABEL: RegExp;
  PAYMENT_DOCS_SELECTOR: string;
  TAX_SUMMARY_CLASS: string;
  TAX_DETAIL_TEXT: RegExp;
  IVA_SEPARADO_PATTERN: RegExp;
  IVA_LINEA_PATTERN: RegExp;
  SELLER_DISCOUNT_TEXT: RegExp;
  TRANSFER_OPTION_TEXT: RegExp;
  NO_ACCOUNT_TEXT: RegExp;
  ORDER_DISCOUNT_TEXT: RegExp;
  PRODUCT_DISCOUNT_SELECTOR: string;
  PRODUCT_DISCOUNT_TEXT: RegExp;
  APPROVAL_PENDING_TEXT: RegExp;
  APPROVAL_BUTTON_TEXT: RegExp;
  APPROVAL_PENDING_BADGE: RegExp;
  VERIFICATION_TEXT: RegExp;
  VALIDATION_TEXT: RegExp;
  PRINT_VOUCHER_TEXT: RegExp;
  SHARE_ORDER_TEXT: RegExp;
  INVOICE_NOT_FOUND: RegExp;
  DISABLE_COMMERCE_EDIT_TEXT: RegExp;
  PENDING_DOCS_TEXT: RegExp;
  POINTS_TEXT: RegExp;
  TASK_LINK_TEXT: RegExp;
  TASK_LABEL_TEXT: RegExp;
  CREDIT_NOTES_TEXT: RegExp;
  CREDIT_NOTES_LABEL: RegExp;
  PROMO_TEXT: RegExp;
  EXTERNAL_ACCESS_TEXT: RegExp;
  BLOCKED_CLIENT_TEXT: RegExp;
  SUGGESTIONS_TEXT: RegExp;
  BETA_TEXT: RegExp;
  NEW_UI_SELECTOR: string;
  FOOTER_SELECTOR: string;
  MIN_ONE_SELECTOR: string;
  MIN_ONE_TEXT: RegExp;
  ADD_BUTTON_LABEL: string;
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create cv-payments.spec.ts — 11 payment and discount tests</name>
  <files>tests/e2e/b2b/config-validation/cv-payments.spec.ts</files>
  <read_first>
    - tests/e2e/b2b/config-validation.spec.ts (lines 287-320 enablePayments, lines 565-589 disablePayments, lines 630-648 enableSellerDiscount, lines 679-700 taxes.showSummary, lines 349-385 includeTaxRateInPrices, lines 1115-1136 enablePaymentsCollection, lines 1140-1159 hasTransferPaymentType, lines 1213-1257 discountTypes.enableOrderDiscountType + enableProductDiscountType, lines 1387-1407 payment.enableNewPaymentModule, lines 1581-1602 paymentsWithoutAccount)
    - tests/e2e/b2b/config-validation/helpers.ts
    - tests/e2e/b2b/config-validation/selectors.ts
  </read_first>
  <action>
Create `cv-payments.spec.ts`:

**Imports:**
```typescript
import { test, expect } from '@playwright/test';
import { skipIfNotInB2B } from '../../fixtures/b2b-feature';
import clients from '../../fixtures/clients';
import { loginIfNeeded, addOneProductToCart } from './helpers';
import { SELECTORS } from './selectors';
```

**Describe title:** `Config validation — Payments: ${client.name}`

**11 tests to migrate:**

1. **enablePayments** (line 287): Replace `page.locator('[class*="payment" i], [class*="pago" i]')` with `page.locator(SELECTORS.PAYMENTS_CLASS)`. Uses ADD_BUTTON_LABEL for adding product inline. Change `loginIfNeeded(page)` to `loginIfNeeded(page, client)`.

2. **disablePayments** (line 565): Same PAYMENTS_CLASS selector. Uses ADD_BUTTON_LABEL. Change `loginIfNeeded(page)` to `loginIfNeeded(page, client)`.

3. **enableSellerDiscount** (line 630): Replace with `page.getByText(SELECTORS.SELLER_DISCOUNT_TEXT).or(page.getByPlaceholder(/descuento.*vendedor|discount/i))`. Uses `addOneProductToCart(page, client)`. Change `loginIfNeeded(page)` to `loginIfNeeded(page, client)`.

4. **taxes.showSummary** (line 679): Replace with `page.locator(SELECTORS.TAX_SUMMARY_CLASS).or(page.getByText(SELECTORS.TAX_DETAIL_TEXT))`. Uses `addOneProductToCart(page, client)`. Change `loginIfNeeded(page)` to `loginIfNeeded(page, client)`.

5. **includeTaxRateInPrices** (line 349): Replace inline IVA regex tests with `SELECTORS.IVA_SEPARADO_PATTERN.test(bodyText || '')` and `SELECTORS.IVA_LINEA_PATTERN.test(bodyText || '')`. Uses ADD_BUTTON_LABEL inline. Change `loginIfNeeded(page)` to `loginIfNeeded(page, client)`.

6. **enablePaymentsCollection** (line 1115): Replace with `page.getByRole('link', { name: SELECTORS.PAYMENT_COLLECTIONS_TEXT }).or(page.getByText(SELECTORS.PAYMENT_COLLECTIONS_LABEL))`. Change `loginIfNeeded(page)` to `loginIfNeeded(page, client)`.

7. **hasTransferPaymentType** (line 1140): Replace with `page.getByText(SELECTORS.TRANSFER_OPTION_TEXT).or(page.getByLabel(SELECTORS.TRANSFER_OPTION_TEXT))`. Uses `addOneProductToCart(page, client)`. Change `loginIfNeeded(page)` to `loginIfNeeded(page, client)`.

8. **discountTypes.enableOrderDiscountType** (line 1213): Replace with `page.getByText(SELECTORS.ORDER_DISCOUNT_TEXT).or(page.locator('[class*="order-discount" i]'))`. Uses `addOneProductToCart(page, client)`. Change `loginIfNeeded(page)` to `loginIfNeeded(page, client)`.

9. **discountTypes.enableProductDiscountType** (line 1236): Replace with `page.locator(SELECTORS.PRODUCT_DISCOUNT_SELECTOR).or(page.getByText(SELECTORS.PRODUCT_DISCOUNT_TEXT))`. Change `loginIfNeeded(page)` to `loginIfNeeded(page, client)`.

10. **payment.enableNewPaymentModule** (line 1387): Replace with `page.locator(SELECTORS.PAYMENT_MODULE_SELECTOR).or(page.locator('[data-testid*="payment" i]'))`. Uses `addOneProductToCart(page, client)`. Change `loginIfNeeded(page)` to `loginIfNeeded(page, client)`.

11. **paymentsWithoutAccount** (line 1581): Replace with `page.getByText(SELECTORS.NO_ACCOUNT_TEXT).or(page.locator('[class*="without-account" i], [class*="withoutAccount" i]'))`. Change `loginIfNeeded(page)` to `loginIfNeeded(page, client)`.

**For every test:** Same rules as Plan 02 -- update helper calls, replace inline selectors, keep all guards/skips/console.warn/annotations.
  </action>
  <verify>
    <automated>cd /Users/lalojimenez/qa && grep -c "test(" tests/e2e/b2b/config-validation/cv-payments.spec.ts && wc -l tests/e2e/b2b/config-validation/cv-payments.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `tests/e2e/b2b/config-validation/cv-payments.spec.ts`
    - `grep -c "test(" tests/e2e/b2b/config-validation/cv-payments.spec.ts` returns 11
    - `wc -l tests/e2e/b2b/config-validation/cv-payments.spec.ts | awk '{print $1}'` is less than 300
    - `grep "Config validation — Payments:" tests/e2e/b2b/config-validation/cv-payments.spec.ts` matches
    - `grep "SELECTORS.PAYMENTS_CLASS" tests/e2e/b2b/config-validation/cv-payments.spec.ts` matches
    - `grep "SELECTORS.SELLER_DISCOUNT_TEXT" tests/e2e/b2b/config-validation/cv-payments.spec.ts` matches
    - `grep "SELECTORS.IVA_SEPARADO_PATTERN" tests/e2e/b2b/config-validation/cv-payments.spec.ts` matches
    - `grep "addOneProductToCart(page, client)" tests/e2e/b2b/config-validation/cv-payments.spec.ts` matches
    - No inline `[class*="payment" i]` selectors remaining
  </acceptance_criteria>
  <done>cv-payments.spec.ts has 11 tests, under 300 lines, uses SELECTORS and helpers</done>
</task>

<task type="auto">
  <name>Task 2: Create cv-orders.spec.ts — 9 order management tests</name>
  <files>tests/e2e/b2b/config-validation/cv-orders.spec.ts</files>
  <read_first>
    - tests/e2e/b2b/config-validation.spec.ts (lines 322-343 ordersRequireAuthorization, lines 970-992 ordersRequireVerification, lines 996-1015 enableOrderValidation, lines 1018-1039 enableOrderApproval, lines 1067-1088 hasVoucherPrinterEnabled, lines 1411-1432 shareOrderNewDesign, lines 1659-1680 enableInvoicesList, lines 1631-1655 enablePaymentDocumentsB2B, lines 1684-1708 disableCommerceEdit)
    - tests/e2e/b2b/config-validation/helpers.ts
    - tests/e2e/b2b/config-validation/selectors.ts
  </read_first>
  <action>
Create `cv-orders.spec.ts`:

**Imports:**
```typescript
import { test, expect } from '@playwright/test';
import { skipIfNotInB2B } from '../../fixtures/b2b-feature';
import clients from '../../fixtures/clients';
import { loginIfNeeded, addOneProductToCart } from './helpers';
import { SELECTORS } from './selectors';
```

**Describe title:** `Config validation — Orders: ${client.name}`

**9 tests to migrate:**

1. **ordersRequireAuthorization** (line 322): Replace `page.getByText(/pendiente|aprobación|autorización|autorizar/i)` with `page.getByText(SELECTORS.APPROVAL_PENDING_TEXT)`. Navigates to `/orders`. Change `loginIfNeeded(page)` to `loginIfNeeded(page, client)`.

2. **ordersRequireVerification** (line 970): Replace with `page.getByText(SELECTORS.VERIFICATION_TEXT).or(page.getByRole('button', { name: /verificar/i }))`. Navigates to `/orders`. Change `loginIfNeeded(page)` to `loginIfNeeded(page, client)`.

3. **enableOrderValidation** (line 996): Replace with `page.getByText(SELECTORS.VALIDATION_TEXT).or(page.locator('[class*="order-validation" i]'))`. Uses `addOneProductToCart(page, client)`. Change `loginIfNeeded(page)` to `loginIfNeeded(page, client)`.

4. **enableOrderApproval** (line 1018): Replace with `page.getByRole('button', { name: SELECTORS.APPROVAL_BUTTON_TEXT }).or(page.getByText(SELECTORS.APPROVAL_PENDING_BADGE))`. Navigates to `/orders`. Change `loginIfNeeded(page)` to `loginIfNeeded(page, client)`.

5. **hasVoucherPrinterEnabled** (line 1067): Replace with `page.getByRole('button', { name: SELECTORS.PRINT_VOUCHER_TEXT }).or(page.getByText(/imprimir.*voucher|print.*voucher/i))`. Navigates to `/orders`. Change `loginIfNeeded(page)` to `loginIfNeeded(page, client)`.

6. **shareOrderNewDesign** (line 1411): Replace with `page.getByRole('button', { name: SELECTORS.SHARE_ORDER_TEXT }).or(page.locator('[class*="share-order" i], [class*="shareOrder" i]'))`. Navigates to `/orders`. Change `loginIfNeeded(page)` to `loginIfNeeded(page, client)`.

7. **enableInvoicesList** (line 1659): Replace `page.getByText(/404|no encontrad|not found/i)` with `page.getByText(SELECTORS.INVOICE_NOT_FOUND)`. Navigates to `/invoices`. Change `loginIfNeeded(page)` to `loginIfNeeded(page, client)`.

8. **enablePaymentDocumentsB2B** (line 1631): Replace with `page.locator(SELECTORS.PAYMENT_DOCS_SELECTOR)` and `page.getByText(SELECTORS.INVOICE_NOT_FOUND)`. Navigates to `/payment-documents`. Change `loginIfNeeded(page)` to `loginIfNeeded(page, client)`.

9. **disableCommerceEdit** (line 1684): Replace with `page.getByRole('button', { name: SELECTORS.DISABLE_COMMERCE_EDIT_TEXT }).or(page.locator('[class*="edit-commerce" i], [class*="editCommerce" i]'))`. Navigates to `/profile`. Change `loginIfNeeded(page)` to `loginIfNeeded(page, client)`.

**For every test:** Same rules -- update helper calls, replace inline selectors, keep all guards/skips/console.warn/annotations.
  </action>
  <verify>
    <automated>cd /Users/lalojimenez/qa && grep -c "test(" tests/e2e/b2b/config-validation/cv-orders.spec.ts && wc -l tests/e2e/b2b/config-validation/cv-orders.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `tests/e2e/b2b/config-validation/cv-orders.spec.ts`
    - `grep -c "test(" tests/e2e/b2b/config-validation/cv-orders.spec.ts` returns 9
    - `wc -l tests/e2e/b2b/config-validation/cv-orders.spec.ts | awk '{print $1}'` is less than 300
    - `grep "Config validation — Orders:" tests/e2e/b2b/config-validation/cv-orders.spec.ts` matches
    - `grep "SELECTORS.APPROVAL_PENDING_TEXT" tests/e2e/b2b/config-validation/cv-orders.spec.ts` matches
    - `grep "SELECTORS.VERIFICATION_TEXT" tests/e2e/b2b/config-validation/cv-orders.spec.ts` matches
    - `grep "SELECTORS.PRINT_VOUCHER_TEXT" tests/e2e/b2b/config-validation/cv-orders.spec.ts` matches
    - `grep "loginIfNeeded(page, client)" tests/e2e/b2b/config-validation/cv-orders.spec.ts` matches
    - No inline `/pendiente|aprobación/` regex remaining
  </acceptance_criteria>
  <done>cv-orders.spec.ts has 9 tests, under 300 lines, uses SELECTORS and helpers</done>
</task>

<task type="auto">
  <name>Task 3: Create cv-ui-features.spec.ts — 12 navigation and UI feature tests</name>
  <files>tests/e2e/b2b/config-validation/cv-ui-features.spec.ts</files>
  <read_first>
    - tests/e2e/b2b/config-validation.spec.ts (lines 703-724 pendingDocuments, lines 728-748 pointsEnabled, lines 752-772 enableTask, lines 776-796 enableCreditNotes, lines 847-868 showMinOne, lines 543-562 footerCustomContent, lines 1317-1338 externalAccess, lines 1341-1363 blockedClientAlert, lines 1367-1383 useNewPromotions, lines 1188-1209 enableBetaButtons, lines 1506-1527 suggestions.hide, lines 1604-1627 enableNewUI)
    - tests/e2e/b2b/config-validation/helpers.ts
    - tests/e2e/b2b/config-validation/selectors.ts
  </read_first>
  <action>
Create `cv-ui-features.spec.ts`:

**Imports:**
```typescript
import { test, expect } from '@playwright/test';
import { skipIfNotInB2B } from '../../fixtures/b2b-feature';
import clients from '../../fixtures/clients';
import { loginIfNeeded } from './helpers';
import { SELECTORS } from './selectors';
```

Note: Does NOT import `addOneProductToCart` -- UI feature tests navigate to home or `/products` directly.

**Describe title:** `Config validation — UI Features: ${client.name}`

**12 tests to migrate:**

1. **pendingDocuments** (line 703): Replace with `page.getByRole('link', { name: SELECTORS.PENDING_DOCS_TEXT }).or(page.getByText(SELECTORS.PENDING_DOCS_TEXT))`. Navigates to home. Change `loginIfNeeded(page)` to `loginIfNeeded(page, client)`.

2. **pointsEnabled** (line 728): Replace with `page.getByText(SELECTORS.POINTS_TEXT).or(page.getByRole('link', { name: /puntos|points/i }))`. Navigates to home. Change `loginIfNeeded(page)` to `loginIfNeeded(page, client)`.

3. **enableTask** (line 752): Replace with `page.getByRole('link', { name: SELECTORS.TASK_LINK_TEXT }).or(page.getByText(SELECTORS.TASK_LABEL_TEXT))`. Navigates to home. Change `loginIfNeeded(page)` to `loginIfNeeded(page, client)`.

4. **enableCreditNotes** (line 776): Replace with `page.getByRole('link', { name: SELECTORS.CREDIT_NOTES_TEXT }).or(page.getByText(SELECTORS.CREDIT_NOTES_LABEL))`. Navigates to home. Change `loginIfNeeded(page)` to `loginIfNeeded(page, client)`.

5. **showMinOne** (line 847): Replace with `page.locator(SELECTORS.MIN_ONE_SELECTOR).or(page.getByText(SELECTORS.MIN_ONE_TEXT))`. Navigates to `/products`. Change `loginIfNeeded(page)` to `loginIfNeeded(page, client)`.

6. **footerCustomContent.useFooterCustomContent** (line 543): Replace `page.locator('footer, [class*="footer" i]')` with `page.locator(SELECTORS.FOOTER_SELECTOR)`. Navigates to home. Change `loginIfNeeded(page)` to `loginIfNeeded(page, client)`.

7. **externalAccess** (line 1317): Replace with `page.getByRole('link', { name: SELECTORS.EXTERNAL_ACCESS_TEXT }).or(page.locator('[class*="external-access" i]'))`. Navigates to home. Change `loginIfNeeded(page)` to `loginIfNeeded(page, client)`.

8. **blockedClientAlert.enableBlockedClientAlert** (line 1341): Replace with `page.getByText(SELECTORS.BLOCKED_CLIENT_TEXT).or(page.locator('[class*="blocked-client" i], [class*="blockedClient" i]'))`. Navigates to home. Change `loginIfNeeded(page)` to `loginIfNeeded(page, client)`.

9. **useNewPromotions** (line 1367): Replace with `page.getByText(SELECTORS.PROMO_TEXT).first()`. Navigates to `/products?promotions=true`. Change `loginIfNeeded(page)` to `loginIfNeeded(page, client)`.

10. **enableBetaButtons** (line 1188): Replace with `page.locator('[class*="beta" i]').or(page.getByText(SELECTORS.BETA_TEXT))`. Navigates to `/products`. Change `loginIfNeeded(page)` to `loginIfNeeded(page, client)`.

11. **suggestions.hide** (line 1506): Replace with `page.getByText(SELECTORS.SUGGESTIONS_TEXT).first().or(page.locator('[class*="suggestion" i], [class*="suggested" i]').first())`. Navigates to `/products`. Change `loginIfNeeded(page)` to `loginIfNeeded(page, client)`.

12. **enableNewUI** (line 1604): Replace with `page.locator(SELECTORS.NEW_UI_SELECTOR)`. Navigates to `/products`. Change `loginIfNeeded(page)` to `loginIfNeeded(page, client)`.

**For every test:** Same rules -- update helper calls, replace inline selectors, keep all guards/skips/console.warn/annotations.
  </action>
  <verify>
    <automated>cd /Users/lalojimenez/qa && grep -c "test(" tests/e2e/b2b/config-validation/cv-ui-features.spec.ts && wc -l tests/e2e/b2b/config-validation/cv-ui-features.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `tests/e2e/b2b/config-validation/cv-ui-features.spec.ts`
    - `grep -c "test(" tests/e2e/b2b/config-validation/cv-ui-features.spec.ts` returns 12
    - `wc -l tests/e2e/b2b/config-validation/cv-ui-features.spec.ts | awk '{print $1}'` is less than 300
    - `grep "Config validation — UI Features:" tests/e2e/b2b/config-validation/cv-ui-features.spec.ts` matches
    - `grep "SELECTORS.PENDING_DOCS_TEXT" tests/e2e/b2b/config-validation/cv-ui-features.spec.ts` matches
    - `grep "SELECTORS.POINTS_TEXT" tests/e2e/b2b/config-validation/cv-ui-features.spec.ts` matches
    - `grep "SELECTORS.CREDIT_NOTES_TEXT" tests/e2e/b2b/config-validation/cv-ui-features.spec.ts` matches
    - `grep "SELECTORS.NEW_UI_SELECTOR" tests/e2e/b2b/config-validation/cv-ui-features.spec.ts` matches
    - `grep "loginIfNeeded(page, client)" tests/e2e/b2b/config-validation/cv-ui-features.spec.ts` matches
    - No inline `/puntos|points|fidelidad/` regex remaining
  </acceptance_criteria>
  <done>cv-ui-features.spec.ts has 12 tests, under 300 lines, uses SELECTORS and helpers</done>
</task>

</tasks>

<verification>
- All 3 files exist in `tests/e2e/b2b/config-validation/`
- Total test count across 3 files: 11 + 9 + 12 = 32 active tests
- Combined with Plan 02 (33 active + 1 skip): 33 + 32 = 65 active + 1 skip = 66 total (matches original)
- Each file under 300 lines
- Every file imports from `./helpers` and `./selectors`
- No inline brittle selectors remain in these 3 files
</verification>

<success_criteria>
- 32 active tests migrated across 3 files
- All files under 300 lines
- All inline selectors replaced with SELECTORS.* constants
- All helper calls updated to explicit `(page, client)` parameters
- Each file independently runnable
</success_criteria>

<output>
After completion, create `.planning/phases/02-config-validation-refactor/02-03-SUMMARY.md`
</output>
