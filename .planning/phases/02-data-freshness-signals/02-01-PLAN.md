---
phase: 2
plan: 01
type: standard
autonomous: true
wave: 1
depends_on: []
requirements: [DASH-01, DASH-02]
---

# Phase 2 Plan 01: CSS + HTML Skeleton for Data Freshness Signals

<objective>
Establish the visual foundation for Phase 2: add the CSS classes (`.client-last-tested`, `.freshness-badge`, `.freshness-badge.freshness-stale`, `.run-nav`, `.run-nav-label`, `.run-select`) and insert the run-nav HTML skeleton (with empty `<select id="runSelector">`) before the `#clientsContainer` div. No JS logic — that belongs to plan 02-02.
</objective>

<context>
@.planning/phases/02-data-freshness-signals/02-CONTEXT.md
@.planning/phases/02-data-freshness-signals/02-UI-SPEC.md
@.planning/phases/02-data-freshness-signals/02-RESEARCH.md
</context>

<tasks>

<task id="1" type="auto">
<name>Add CSS classes to the existing &lt;style&gt; block in public/index.html</name>

Insert, right after the `.client-url` rule (~line 572), a block of CSS containing:
- `.client-last-tested` — 11px/400/#9ca3af, margin-top 4px
- `.freshness-badge` base (inline-flex, padding 4px 8px, border-radius 9999px, 11px/700)
- `.freshness-badge.freshness-stale` — background #fef3c7, color #92400e
- `.run-nav` — flex, gap 8px, margin-bottom 8px
- `.run-nav-label` — 12px/400/#6b7280
- `.run-select` — 12px select styling with focus state (#667eea)

Exact CSS is in `02-UI-SPEC.md` Component Inventory section (lines 112-118, 144-157, 188-213) and `02-RESEARCH.md` Code Examples section (lines 349-394).

Do not modify any existing CSS rule. Do not change the `.client-card-header`, `.client-card-body`, or any existing selector.

<verification>
- `grep -n 'client-last-tested' public/index.html` returns at least one match in the `<style>` block (before line 800).
- `grep -n 'freshness-badge' public/index.html` returns at least one match.
- `grep -n 'run-select' public/index.html` returns at least one match.
- `grep -n '.client-card-header' public/index.html` still returns the original match (unchanged).
</verification>

<done_when>
All 6 CSS classes exist in the `<style>` block of `public/index.html`, the existing rules remain untouched, and the file parses (a quick grep for duplicate selectors yields none).
</done_when>
</task>

<task id="2" type="auto">
<name>Insert run-nav HTML skeleton before #clientsContainer</name>

Inside the existing `<div class="card">` that holds "🏪 Clientes B2B" (around line 1218–1223), insert a new `<div class="run-nav">` right before the `<div class="clients-grid" id="clientsContainer">`. The structure:

```html
<div class="run-nav">
    <span class="run-nav-label">Run:</span>
    <select id="runSelector" class="run-select"></select>
</div>
```

The `<select>` is left empty — plan 02-02 populates it via JS. Keeping it empty now means the UI still renders cleanly (the label + empty select is visible but non-functional; it becomes functional when 02-02 lands).

Do not touch any other HTML. Do not modify the card-title, the clients-grid, or anything else in the document body.

<verification>
- `grep -n 'id="runSelector"' public/index.html` returns exactly one match.
- `grep -n 'class="run-nav"' public/index.html` returns exactly one match.
- `grep -n 'clientsContainer' public/index.html` still returns the existing two matches (the HTML line ~1220 and the JS reference).
- The new `<div class="run-nav">` precedes `<div class="clients-grid" id="clientsContainer">` in the file (manual verification — read the surrounding 10 lines).
</verification>

<done_when>
The `<div class="run-nav">` with its label and empty `<select id="runSelector" class="run-select">` sits directly before `<div class="clients-grid" id="clientsContainer">` inside the B2B `<div class="card">`. Nothing else in the HTML is modified.
</done_when>
</task>

</tasks>

<success_criteria>
- CSS for `.client-last-tested`, `.freshness-badge`, `.freshness-badge.freshness-stale`, `.run-nav`, `.run-nav-label`, `.run-select` is present in `public/index.html`.
- Run-nav HTML skeleton (label + empty `<select>`) is inserted before `#clientsContainer` inside the B2B card.
- No existing CSS rule, JS function, or HTML structure is modified (other than the two additive insertions above).
- Dashboard still renders without JS errors when loaded in a browser (visual sanity: run-nav shows a "Run:" label with an empty dropdown).
</success_criteria>

<output>
- Commit 1: `feat(02-01): add CSS classes for freshness signals and run nav`
- Commit 2: `feat(02-01): insert run-nav HTML skeleton before clients grid`
- SUMMARY.md committed with `docs(02-01): complete CSS + HTML skeleton plan`
</output>
